#include <stdio.h>
#include <string.h>
int main() {
    char str[] = "madam", rev[6];
    strcpy(rev, str);
    strrev(rev);
    if (strcmp(str, rev) == 0)
        printf("Palindrome\n");
    else
        printf("Not Palindrome\n");
    return 0;
}
